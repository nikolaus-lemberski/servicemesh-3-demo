kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: service-a
spec:
  to:
    kind: Service
    name: service-a
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
